
$(document).ready(function(){
    /* 메뉴 바로 뒤에는 로그인 버튼이 있음
       로그인 버튼에 포커스가 되면 메뉴의 오버상태를 해제 */
    
    // $('.header .gnb .gnb_btn span').on('click', function(){
    //     $('.header').toggleClass('menu_open')
    // })
    // $('.header .gnb ul.depth1 > li > a').on('click', function(){
    //     e.preventDefault();
    //     let open_status = $(this).parent().hasClass('sub_open')
    //     console.log(open_status)
    //     if(open_status == true){
    //         $(this).parents().removeClass('sub_open')
    //     }else{
    //         $(this).parents().addClass('sub_open')

        }
        })

	});
